# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# env files: real values live in Vercel and .env.local, never in git
.env
.env.local
.env*.local

# member data: this repo is public, and the roster is the credential.
# /card authenticates on member number plus last name, so publishing the
# sheet would hand out every card. Real files stay local and in Supabase.
/documents/

# typescript
*.tsbuildinfo
